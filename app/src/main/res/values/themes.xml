<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DukaShare" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryLightColor</item>
        <item name="colorOnPrimary">@color/primaryTextColor</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryLightColor</item>
        <item name="colorOnSecondary">@color/secondaryTextColor</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="colorButtonNormal">@color/lightGrey</item>


    </style>

    <style name="PrimaryButtonTheme" parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/PrimaryButtonStyle</item>
    </style>

    <style name="SecondaryButtonTheme" parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/SecondaryButtonStyle</item>
    </style>

    <style name="PrimaryButtonStyle">
        <item name="colorPrimary">@color/primaryButtonColor</item>
        <item name="colorOnPrimary">@color/secondaryLightColor</item>
    </style>

    <style name="SecondaryButtonStyle">
        <item name="colorPrimary">@color/lightGrey</item>
        <item name="colorOnPrimary">@color/secondaryTextColor</item>
    </style>

    <style name="Theme.DukaShare.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.DukaShare.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.DukaShare.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>